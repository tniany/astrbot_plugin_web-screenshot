{
    "command_alias": {
        "description": "自定义指令别名，设置后将替换默认的 /截图 指令",
        "type": "string",
        "default": ""
    },
    "pure_mode": {
        "description": "纯净模式：开启后截图只返回图片，不返回文字内容",
        "type": "bool",
        "default": false
    }
}
